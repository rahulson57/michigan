/**
 * PLACEHOLDER — owned by the editor task, which replaces this whole file.
 * The route is already wired in client/src/App.jsx; nothing outside this file
 * needs to change for the real page to light up.
 */
export default function EditorPage() {
  return (
    <div className="container-narrow section stack">
      <hr className="rule-accent" />
      <h1 className="page-title">Write a story</h1>
      <p className="lede">The TipTap editor, cover upload, tags, and draft/publish controls live here.</p>
      <p className="meta">Placeholder — the editor task owns this screen.</p>
    </div>
  );
}
